import { Container, Paper } from '@mui/material';

export const Main = () => {
  return (
    <Container fixed>
        <Paper style={{ padding: '1rem' }}>
          main
        </Paper>
    </Container>
  );
};
